version: "3.9"

services:
  chatapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatapp-devsecops
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
